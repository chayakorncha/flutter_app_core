import 'package:app_core/src/config/index_app.dart';

class L10n {
  static const Locale th = Locale('th', 'TH');
  static const Locale en = Locale('en', 'US');
  static const supportLangStrings = ['en', 'th'];

  static final supportLang = [th, en];
}
